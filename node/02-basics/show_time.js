let time = new Date();

let hours = time.getHours();
let minutes = time.getMinutes();

let output = `${hours}:${minutes}`;

console.log(output);

// todo: assignement make this time appear in am/pm format
